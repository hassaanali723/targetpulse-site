// Single switch for the Mimecast spoke page. It controls the page itself (404
// when off), its sitemap entry, its footer link, the nav Solutions dropdown
// entry, and the hub -> Mimecast contextual link, so they all turn on together.
//
// The page is now released, so this is on. Flip to false to pull it back down.
export const MIMECAST_PAGE_LIVE = true
